# Matterarc Standard Release

This repository is the public build-only release channel for the desktop app.

It contains:
- the current release manifest

It does not contain private source code or client data.

The setup bootstrapper and full offline installer are distributed as GitHub release assets and referenced in `latest.json`.